name: Maintenance

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: maintenance-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update_and_lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Update flake lock
        run: nix flake update

      - name: Install Rust toolchain
        run: nix develop . --command rustup toolchain install --profile minimal stable --component rustfmt --component clippy --target wasm32-unknown-unknown

      - name: Update dependencies and lint
        run: nix develop . --command cargo run --locked --no-default-features --manifest-path tools/site/Cargo.toml -- update

      - name: Commit maintenance changes
        run: nix develop . --command cargo run --locked --no-default-features --manifest-path tools/site/Cargo.toml -- commit update_and_lint
