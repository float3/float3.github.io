[package]
name = "site"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
image = { version = "0.25.8", default-features = false, features = ["jpeg", "png", "webp"] }

[[bin]]
name = "site"
path = "src/main.rs"
